#include <iostream>
using namespace std;

int main() {
    int x = 5;

    if (x <= 22) {
        int y = 12;
    }

    //cout << y;  //Doesn't exist outside of local if statement. Could be if changed to global (declared at top outside any function including main)

}